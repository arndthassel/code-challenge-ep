/// The amount of items loaded into the network/stream list.
const kDynamicListSize = 10;

/// The amount of seconds it takes to load a fake item.
const kFetchSeconds = 1;

/// The amount of items loaded into a long list.
const kLongListSize = 10000;

/// The amount of items loaded into a small list.
const kSmallListSize = 5;
